(function (window) {
    'use strict';
    window.opspark = window.opspark || {};
    window.opspark.platform = window.opspark.platform || {};
    
    let platform = window.opspark.createPlatform
        createPlatform(400, 250);
        createPlatform(200,200,0.3)
        createPlatform(300,315,0.4)
        createPlatform(100,130,0.3)
        createPlatform(450,300)
    
    }
    platform.init = init;
})(window);